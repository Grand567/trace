import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.travelassistant',
  appName: 'travel-assistant',
  webDir: 'dist'
};

export default config;
